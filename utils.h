/*
utils.h
Author: Jeff Hansen
Same header stuff as in the main file.. you get the idea.
*/

void nuke(char* buf);
char* speed(char* buf, int voltage, int* kph);
char* total(char* buf, int* totalKm, int kph);